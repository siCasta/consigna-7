# Consigna 7 para el curso de Coderhouse.

## Rutas

- `GET` /products
- `GET` /products/create
- `GET` /api/products
- `POST` /api/products/
- `GET` /api/chat
- `POST` /api/chat

## Uso

```bash
$ npm i
$ npm start
```